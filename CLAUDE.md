# CLAUDE.md

Project rules live in `AGENTS.md`. Read that first — all of it applies to Claude
Code unchanged.

This file is the Claude-side overlay. It is deliberately empty of rules today:
everything currently documented is provider-neutral. Add a rule here only when it
genuinely differs for Claude Code; anything else belongs in `AGENTS.md`.
